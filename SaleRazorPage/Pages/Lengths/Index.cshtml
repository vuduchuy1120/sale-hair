@page
@model SaleRazorPage.Pages.Lengths.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Length[0].Inch)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Length[0].Size)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Length[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Length[0].Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Length) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Inch)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Size)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
